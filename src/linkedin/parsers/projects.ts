import type { RscResponse } from './rsc.js';

export function parseProjects(_response: RscResponse): unknown[] {
  // Phase 2: map LinkedIn's SDUI/RSC entities into normalized records.
  return [];
}
